apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ecommerce-demo-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ecommerce-demo-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ecommerce-demo-chart.fullname" . }}:{{ .Values.catalog.servicePort }}']
  restartPolicy: Never
